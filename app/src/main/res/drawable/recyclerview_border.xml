<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Arka plan rengi (şeffaf) -->
    <stroke
        android:width="2dp"
    android:color="#FF0000" />
</shape>
